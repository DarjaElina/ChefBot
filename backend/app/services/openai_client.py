# app/services/openai_client.py
# This file handles communication with OpenAI's API.
# It sends the user input, builds the prompt, and returns the AI's response.
# Keeps the AI logic separate from the route handlers.
